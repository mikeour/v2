{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "SPOTIFY_CLIENT_ID",
    "SPOTIFY_CLIENT_SECRET",
    "SPOTIFY_REFRESH_TOKEN",
    "LASTFM_API_KEY",
    "LASTFM_USERNAME"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!**/*.stories.{tsx,jsx,ts,js}"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "build:storybook": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "check": {
      "dependsOn": ["^build"]
    },
    "typecheck": {},
    "fix": {},
    "clean": {
      "cache": false
    }
  }
}
